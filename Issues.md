# Issues

GUI
- needs more utiluty buttons
- should adapt different approaches in adding a new entry (modal)

Database
- do NOT refresh the player info every game
- do NOT store equip IDs to database
- but still provides APIs to identify the player ID

UDP socket
- needs an user interface (=GUI) for UDP receive IP and broadcast IP

Install script
- needs to be able to be run on every common OS (Windows, Mac, Linux)